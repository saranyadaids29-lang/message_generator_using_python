# message_generator_using_python
This Python program is a simple message management system. It allows users to send, view, and delete messages using a menu. Messages are stored in a list called messages. The program uses a while loop and conditional statements to control user choices. It continues running until the user selects the exit option.
